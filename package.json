{
  "name": "produktionstool",
  "version": "1.0.0",
  "private": true,
  "description": "Produktionstool monorepo: Electron app + API backend",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w nightclub-production-tool",
    "dev:electron": "npm run dev -w nightclub-production-tool",
    "dev:db-viewer": "npm run dev -w db-viewer",
    "start:electron": "npm run start -w nightclub-production-tool",
    "start:backend": "npm run start -w produktionstool-api",
    "build:db-viewer": "npm run build -w db-viewer",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "docker:clean": "docker compose down -v && docker rm -f produktionstool-adminer-1 produktionstool-pgadmin-1 2>/dev/null || true && docker network rm produktionstool_default 2>/dev/null || true",
    "migrate": "docker compose run --rm --build api npm run migrate"
  },
  "keywords": ["produktionstool", "electron", "backend"],
  "license": "MIT"
}
