# API port (default 3000)
PORT=3001

# Postgres connection string (used in Phase 1 step 2+)
DATABASE_URL=postgres://produktionstool:produktionstool@localhost:5432/produktionstool

# Root path for PDF/Excel storage (used in Phase 1 step 3+)
# STORAGE_PATH=./storage

# For Docker: docker-compose.yml sets PORT, DATABASE_URL, STORAGE_PATH for the api service.
# From repo root: docker compose up -d && curl http://localhost:3001/api/health
